<%= form_for(@machine) do |f| %>
  <% if @machine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@machine.errors.count, "error") %> prohibited this machine from being saved:</h2>

      <ul>
      <% @machine.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :floor %><br>
    <%= f.select :floor, options_for_select(1..6) %>
  </div>
  <div class="field">
    <%= f.label :machineId %><br>
    <%= f.text_area :machineId %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
